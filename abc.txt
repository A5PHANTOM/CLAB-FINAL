program (lex lex pgm.l ,lex.yy.c ,./a.out )
%{
#include<stdio.h>
#include<stdlib.h>
int i;
%}

%%
[a-zA-Z]+ {
for (i=0;i<=yyleng-3;i++)
{
if(yytext[i]=='a'&&yytext[i+1]=='b'&&yytext[i+2]=='c'){
yytext[i]='A';
yytext[i+1]='B';
yytext[i+2]='C';
}
}
printf("%s",yytext);
}
[ \t]+       ;        /* ignore spaces and tabs */
\n           { printf("\n"); }
.            { ECHO; }
%%
int main(){
yylex();
return 0;
}
int yywrap()
{
return 1;
}

output

abc
ABC
